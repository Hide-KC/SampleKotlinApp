<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:background">@color/toolbar_back</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="PopupToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@color/toolbar_back</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="ImageTheme">
        <item name="android:background">@drawable/image_frame</item>
        <item name="android:bottomLeftRadius">@dimen/image_corner_radius</item>
        <item name="android:bottomRightRadius">@dimen/image_corner_radius</item>
        <item name="android:topLeftRadius">@dimen/image_corner_radius</item>
        <item name="android:topRightRadius">@dimen/image_corner_radius</item>
    </style>

</resources>
